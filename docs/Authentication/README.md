# Authentication UI reference

Screenshots of **login** (`/login`) and **registration** (`/register`) in Google Chrome (`localhost`). Same purpose as other `docs/` image sets: **repository viewers** see the public entry screens without running the app. Not bundled into the frontend build.

Files are in **`images/`** next to this README.

| File | Route / role | What it shows |
|------|----------------|---------------|
| `login-student.png` | `/login`, Student | Student ID + password, role tabs |
| `login-faculty.png` | `/login`, Faculty | Faculty ID + password |
| `login-admin.png` | `/login`, Admin | Admin ID + password |
| `register-student.png` | `/register`, Student | Student signup (confirm password, disclaimer) |
| `register-faculty.png` | `/register`, Faculty | Faculty signup |

The root **[README](../../README.md)** embeds these under **UI screenshots → Login & registration**.
